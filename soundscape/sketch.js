
    let labSound;

function preload() {

  labSound = loadSound ("assets/lab.wav");
}

    function setup() {
      createCanvas(windowWidth, windowHeight);
    
    }

    function draw() {
      background("hotpink");
    }
